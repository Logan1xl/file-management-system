package com.example.filecomponentsystem.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class ComponentConfiguration {
    // La configuration est gérée par les annotations @Component et l'injection de dépendances
    // Spring Boot détecte automatiquement les composants grâce à @SpringBootApplication
}